import { CWrapper } from '@/components/ui/CWrapper';
import { View } from 'react-native';

export default function StatisticPage() {
    return (
        <CWrapper>
            <View>to be continued...</View>
        </CWrapper>
    );
}
